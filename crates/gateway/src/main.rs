//! HTTP + MCP gateway (Axum + tower) for Taiwan Data Hub
//!
//! Placeholder binary — implementation pending. See `docs/DESIGN.md`.

fn main() {
    eprintln!(
        "{} v{} — placeholder binary; implementation pending.",
        env!("CARGO_PKG_NAME"),
        env!("CARGO_PKG_VERSION"),
    );
}
