# Minimal Prometheus config — scrapes the OTel collector's prom
# exporter. Crates will register their own scrape jobs as they start
# exposing /metrics.
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: otel-collector
    static_configs:
      - targets: ["otel-collector:8888"]
